[project]
name = "aissemble-summarization-example"
version = "1.5.0.dev"
description = "Example demonstrating text summarization with aiSSEMBLE Inference"
readme = "README.md"
authors = [
    { name = "aiSSEMBLE Team", email = "aissemble@bah.com" }
]
requires-python = ">=3.11"
dependencies = [
    "aissemble-inference-core",
    "aissemble-inference-sumy",
    "requests>=2.31.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.sdist]
exclude = ["target/"]

[tool.ruff.lint]
ignore = ["E741"]

[tool.uv.sources]
aissemble-inference-core = { path = "../../aissemble-inference-core", editable = true }
aissemble-inference-sumy = { path = "../../aissemble-inference-modules/aissemble-inference-sumy", editable = true }
aissemble-inference-common-test = { path = "../../aissemble-inference-modules/aissemble-inference-common-test", editable = true }

[[tool.uv.index]]
name = "pypi"
url = "https://pypi.org/simple/"

[dependency-groups]
dev = [
    "ruff>=0.14.9",
    "behave>=1.2.6",
    "mlserver>=1.6.0",
    "sumy>=0.11.0",
    "nltk>=3.8",
    "kappa-maki>=1.0.2",
    "aissemble-inference-common-test",
]

test = [
    "behave>=1.2.6",
    "mlserver>=1.6.0",
    "sumy>=0.11.0",
    "nltk>=3.8",
    "aissemble-inference-common-test",
]
